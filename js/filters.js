// import { getData } from './api.js';

// const filtersElement = document.querySelector('.map__filters');

// const filterOffers = getData().then((data) => data.slice(0, 10));

// export { filterOffers };
